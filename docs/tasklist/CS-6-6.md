# Tasklist: CS-6-6 -- Large Module Decomposition

**Ticket:** CS-6-6
**Status:** IMPLEMENT_STEP_OK
**Created:** 2026-03-02

## Context

Phase 6 reduces the 5 remaining 1000+ line files by extracting inline test modules to sibling files, splitting `main.rs` into focused modules, extracting Dart's error recovery subsystem, and deduplicating `find_capture` across 14 parser files. This is purely structural/cleanup work with no behavioral changes. Each sub-task must be committed independently after passing all checks.

**Target file size reductions:**
- `dart.rs`: 1860 -> ~855 lines (-68%)
- `csharp.rs`: 1431 -> ~517 lines (-64%)
- `cpp.rs`: 1305 -> ~574 lines (-56%)
- `typescript.rs`: 1200 -> ~802 lines (-33%)
- `main.rs`: 1043 -> ~330 lines (-68%)

---

## Task 6.1: Extract Parser Tests to Separate Files

### 6.1.1 Extract `dart.rs` tests to `dart_tests.rs`

- [x] Create `src/parsers/treesitter/dart_tests.rs` with the body of the test module (starting with `use super::*;`, all `#[test]` functions, no wrapping `mod tests { }`), extracted from `dart.rs` L1095-1860 (766 lines). Replace the inline `#[cfg(test)] mod tests { ... }` block in `dart.rs` with `#[cfg(test)] #[path = "dart_tests.rs"] mod tests;`. Commit after passing `cargo test parsers::treesitter::dart && cargo clippy -- -D warnings && cargo fmt --check`.

**Acceptance criteria:**
- `dart.rs` is reduced to ~1094 lines and `dart_tests.rs` exists with 766 lines of test code.
- `cargo test parsers::treesitter::dart` passes with zero failures.

### 6.1.2 Extract `csharp.rs` tests to `csharp_tests.rs`

- [x] Create `src/parsers/treesitter/csharp_tests.rs` with the body of the test module extracted from `csharp.rs` L518-1431 (914 lines). Replace the inline test block with `#[cfg(test)] #[path = "csharp_tests.rs"] mod tests;`. Commit after passing checks.

**Acceptance criteria:**
- `csharp.rs` is reduced to ~517 lines and `csharp_tests.rs` exists with 914 lines of test code.
- `cargo test parsers::treesitter::csharp` passes with zero failures.

### 6.1.3 Extract `cpp.rs` tests to `cpp_tests.rs`

- [x] Create `src/parsers/treesitter/cpp_tests.rs` with the body of the test module extracted from `cpp.rs` L574-1305 (731 lines). Replace the inline test block with `#[cfg(test)] #[path = "cpp_tests.rs"] mod tests;`. Commit after passing checks.

**Acceptance criteria:**
- `cpp.rs` is reduced to ~574 lines and `cpp_tests.rs` exists with 731 lines of test code.
- `cargo test parsers::treesitter::cpp` passes with zero failures.

### 6.1.4 Extract `typescript.rs` tests to `typescript_tests.rs`

- [x] Create `src/parsers/treesitter/typescript_tests.rs` with the body of the test module extracted from `typescript.rs` L803-1200 (398 lines). Replace the inline test block with `#[cfg(test)] #[path = "typescript_tests.rs"] mod tests;`. Commit after passing checks.

**Acceptance criteria:**
- `typescript.rs` is reduced to ~802 lines and `typescript_tests.rs` exists with 398 lines of test code.
- `cargo test parsers::treesitter::typescript` passes with zero failures.

---

## Task 6.2: Split `main.rs`

### 6.2.1 Move `Cli`, `Commands`, `find_project_root()` to `src/cli.rs`

- [x] Create `src/cli.rs` (~660 lines) containing `pub struct Cli` (from `main.rs` L7-107), `pub enum Commands` (from `main.rs` L109-620), and `pub fn find_project_root()` (from `main.rs` L1000-1043). Add `mod cli;` and `use cli::{Cli, Commands, find_project_root};` in `main.rs`. Remove the moved definitions from `main.rs`. Commit after passing `cargo build && cargo test && cargo clippy -- -D warnings && cargo fmt --check`.

**Acceptance criteria:**
- `src/cli.rs` exists with `pub struct Cli`, `pub enum Commands`, and `pub fn find_project_root()`.
- `main.rs` is reduced to ~383 lines; all CLI commands behave identically and `cargo build` succeeds.

### 6.2.2 Move `cmd_install_claude_plugin()` to `management.rs`

- [x] Add `pub fn cmd_install_claude_plugin() -> Result<()>` to `src/commands/management.rs` (copied from `main.rs` L947-998). Remove the definition from `main.rs`. Update the dispatch in `main.rs` to call `commands::management::cmd_install_claude_plugin()`. Commit after passing checks.

**Acceptance criteria:**
- `cmd_install_claude_plugin()` is defined as `pub fn` in `src/commands/management.rs` and no longer defined in `main.rs`.
- The `install-claude-plugin` command works identically and `cargo test` passes.

---

## Task 6.3: Extract Dart Error Recovery Submodule

### 6.3.1 Extract Dart error recovery to `dart_error_recovery.rs` (depends on 6.1.1)

- [x] Create `src/parsers/treesitter/dart_error_recovery.rs` (~240 lines) containing all structs and functions from `dart.rs` post-test L855-1093 (`try_recover_from_error`, `ClassInfo`, `ExtTypeInfo`, `try_parse_modified_class`, `parse_parents_from_class_text`, `try_parse_extension_type`, `extract_parents_from_error_text`, `find_first_identifier`, `find_first_type_identifier`, `find_descendant_by_kind`). Expose shared helpers as `pub(super)`. Change `walk_body_declarations` in `dart.rs` to `pub(super)`. Add `mod error_recovery;` in `dart.rs` and update all call sites to use `error_recovery::` prefix. Commit after passing `cargo test parsers::treesitter::dart && cargo clippy -- -D warnings && cargo fmt --check`.

**Acceptance criteria:**
- `dart_error_recovery.rs` exists and `dart.rs` is reduced to ~855 lines with no local definitions of the moved functions.
- All Dart parser tests pass and error recovery behavior is unchanged.

---

## Task 6.4: Deduplicate `find_capture`

### 6.4.1 Add shared `find_capture` to `mod.rs` and remove 14 local copies (depends on 6.1.1-6.1.4)

- [x] Add `pub(crate) fn find_capture<'a>(m: &'a tree_sitter::QueryMatch<'a, 'a>, idx: Option<u32>) -> Option<&'a tree_sitter::QueryCapture<'a>>` to `src/parsers/treesitter/mod.rs`. Remove the local `fn find_capture` definition from all 14 parser files (`cpp.rs`, `csharp.rs`, `go.rs`, `java.rs`, `kotlin.rs`, `objc.rs`, `php.rs`, `proto.rs`, `python.rs`, `ruby.rs`, `rust_lang.rs`, `scala.rs`, `swift.rs`, `typescript.rs`). Add `find_capture` to each file's existing `use super::` import. Commit after passing `cargo test && cargo clippy -- -D warnings && cargo fmt --check`.

**Acceptance criteria:**
- `find_capture` is defined exactly once in `src/parsers/treesitter/mod.rs` as `pub(crate)` and no local copies remain in any of the 14 parser files.
- `cargo test` passes with all parser tests green.

---

## Task 6.5: Documentation Updates

### 6.5.1 Update `CLAUDE.md` architecture section

- [x] Update the architecture section in `CLAUDE.md` to document the new file layout: new `src/cli.rs` module (`Cli`, `Commands`, `find_project_root()`), `cmd_install_claude_plugin()` relocated to `src/commands/management.rs`, the four new `*_tests.rs` files using the `#[path]` pattern, `src/parsers/treesitter/dart_error_recovery.rs` submodule, and shared `pub(crate) find_capture` in `treesitter/mod.rs`. Update `main.rs` description to reflect ~330 lines focused on dispatch. Commit.

**Acceptance criteria:**
- `CLAUDE.md` references `cli.rs`, `dart_error_recovery.rs`, all four `*_tests.rs` files, and the shared `find_capture` in `mod.rs`.
- The `main.rs` description is updated to reflect its reduced (~330 line) dispatch-only role.

### 6.5.2 Update `docs/tasklist.md` for Phase 6

- [x] Update `docs/tasklist.md` to mark all Phase 6 task checkboxes as complete (`[x]`) and update Phase 6 status to "Complete". Commit.

**Acceptance criteria:**
- All Phase 6 checkboxes in `docs/tasklist.md` are checked (`[x]`).
- Phase 6 status is set to "Complete".

---

## Full Acceptance (after all tasks)

Run:
```bash
cargo fmt --check && cargo clippy -- -D warnings && cargo clippy --tests -- -D warnings && cargo test && cargo test --test memory_tests -- --test-threads=1
```

All checks must pass with zero errors and zero warnings. File size verification:
```bash
wc -l src/main.rs src/parsers/treesitter/dart.rs src/parsers/treesitter/csharp.rs src/parsers/treesitter/cpp.rs src/parsers/treesitter/typescript.rs
```

Expected: all under 1000 lines (`main.rs` ~330, `dart.rs` ~855, `csharp.rs` ~517, `cpp.rs` ~574, `typescript.rs` ~802).
